# 正式发版签名证书的 SHA-256 指纹（小写十六进制，无冒号）。
#
# Android 不允许跨签名覆盖安装：装在手机上的包和新包必须是同一张证书签出来的，
# 否则只能先卸载（本地数据一起没）。v2.1.0 到 v4.0.1 每一版都是构建机现场随机
# 生成的 debug 密钥，互不相同；v4.1.0 起改用固定密钥，指纹就是下面这个值。
#
# 它被 .github/workflows/release.yml 当成硬门禁：构建出的 APK 指纹与此不符就不发版。
# 只有真的换了签名密钥（并且接受所有老用户必须卸载重装）才应该改动这一行。
#
# 来源：v4.1.0 发版说明，且已用 APK Signing Block v2 的证书 DER 独立复核。
c659446f910f8b43b0b5843f10fa3e1fd2d5782b07bbeedf452cecb27bc095e3
